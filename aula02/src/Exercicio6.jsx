export default function({capital}){

    capital = Number(capi);
    juros =  Number(taxa);
    tempo = Number(meses);

    let jurosS = capi * (1 + (taxa/100) * meses);
    let jurosC = capi * (1 + (taxa/100) * (meses* meses));
}